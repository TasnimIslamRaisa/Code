#include<stdio.h>
int main()
{
    int n,x,y,i,sum;
    sum=0;
    scanf("%d",&n);
    for(i=n;i<=x;i++){
        scanf("%d%d",&x,&y);
       if(x%2!=0)
            sum=sum+x;


    }
     printf("%d\n",x);

    return 0;
}
